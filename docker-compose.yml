version: '3.8'

services:
  docs:
    build:
      context: .
      dockerfile: Dockerfile
    image: koyuki-studyhub:latest
    container_name: koyuki-studyhub
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
