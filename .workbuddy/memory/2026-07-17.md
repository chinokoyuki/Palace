# 2026-07-17

## Python Grammar 文章批量优化

**任务：** 审查并优化 `docs/book/python/grammar/` 下全部 19 篇 Python 教程文章。

### 格式转换
- 将 10 个文件中的 `> blockquote` 格式全部转为 `:::tip` / `:::warning` 容器格式：
  - `2_basic_syntax.md`, `3_comments.md`, `7_lists.md`, `8_dictionaries.md`, `11_formatted_strings.md`, `12_functions.md`, `14_classes.md`, `15_files.md` → `:::tip`
  - `13_importing_modules.md`, `18_modules.md` → `:::warning`（涉及不推荐的实践）

### 内容优化
1. **`11_formatted_strings.md`**：修复 `format()` 示例中遗留的 `A{year}B` 占位符残留（源自原始笔记），改为自然的 `"{year}年，你好{name}！"` 形式
2. **`9_for_loops.md`**：新增 `for...else` 小节，与 `while...else` 保持一致
3. **`10_while_loops.md`**：补充 `while...else` 中 `break` 打断场景的示例和说明
4. **`4_data_types.md`**：为完整示例中的 `type().\_\_name\_\_` 添加注释说明
5. **`12_functions.md`**：新增 `lambda` 匿名函数小节
6. **`14_classes.md`**：新增 `@classmethod` 和 `@staticmethod` 小节

## C++ Grammar 新增章节

新增两章 C++ 语法教程，沿袭现有章节风格（frontmatter + 代码示例 + 运行结果 + 完整示例）：

### `15_memory_model.md` — C++ 内存模型
- 五段内存分区：栈、堆、全局/静态区、常量区、代码区
- 栈 vs 堆详细对比（速度、空间、生命周期、管理方式）
- 动态数组的内存分配、内存泄漏成因与防范
- `:::warning` 和 `:::tip` 容器提示关键注意事项

### `16_references.md` — C++ 引用
- 引用的本质（别名）、定义与初始化规则
- 引用 vs 指针的七维度对比表
- 函数参数中的引用传递、const 引用（含绑定临时对象）
- 返回值引用的正确与错误用法（`:::danger` 标注陷阱）
- 左值引用与右值引用的简要区分

## 新增"计算机"分类 &《数据结构》书

在 `books.ts` 中新增 `计算机` 分类（BookCategory），内含《数据结构》书，侧边栏路径 `/book/ds/`。

### 目录结构
```
docs/book/ds/intro/
├── 1_1_basic_concepts.md   # 1.1 数据结构的基本概念
└── 1_2_algorithm.md         # 1.2 算法和算法评价
```

### `1_1_basic_concepts.md` — 数据结构的基本概念
- 基本概念与术语：数据、数据元素、数据项、数据对象、数据结构（渐进定义）
- 三要素：逻辑结构（线性/树形/图形/集合 + 表格）、存储结构（顺序/链式/索引/散列 + 优缺点对比）、数据运算（六种常见运算 + 运算与存储的关系）
- `:::tip` 通俗比喻和关键提醒

### `1_2_algorithm.md` — 算法和算法评价
- 算法基本概念：定义、五大特性（表格）、好算法标准
- 时间复杂度：问题规模、频度、大 O 记号推导三步法、8 级常见复杂度表格 + n=100 增长对比、5 个计算示例（O(1)/O(n)/O(n²)/O(log n)/多段综合）
- 空间复杂度：辅助空间、O(1)/O(n)/递归 O(n) 示例
- 时间 vs 空间取舍哲学
- 完整示例：同一问题 O(n²) vs O(n log n) vs O(n) 三种方案对比
